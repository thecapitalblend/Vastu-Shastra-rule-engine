# AutoCAD / 3D Workflow

Master geometry is stored in millimetres. The DXF export creates a plot boundary and room rectangles when their coordinates fit inside the plot.

AI images are visualization outputs. Construction dimensions must come from the approved CAD/BIM model, not from generated pixels.
